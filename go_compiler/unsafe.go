package main

import (
	"fmt"
)

func main() {
	fmt.Println("Go Compiler")

	/*You can compile a Go source file using the go tool compile command. What you will
	get is an object file, which is a file with the .o file extension.*/

	/*An object file is a binary file that contains object code, which is machine code in a
	relocatable format*/

	/*An archive file is a binary file that contains one or more files that is primarily used for
	grouping multiple files into a single file. The archive format used by Go is called ar.
	*/

	// go tool compile commnad allows to to detect race conditions


}
